import { describe, it, expect } from 'vitest'
import Button from 'src/elements/Button/Button'

describe('Button import test', () => {
  it('should import Button successfully', () => {
    expect(Button).toBeDefined()
  })
})
